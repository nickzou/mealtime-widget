import React from 'react';
import ReactDOM from 'react-dom';
import Wrapper from './components/Wrapper';

const appContainer = document.getElementById('mealtime');

ReactDOM.render(<Wrapper />, appContainer);
